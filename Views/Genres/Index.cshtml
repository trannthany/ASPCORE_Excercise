@model IEnumerable<WebApplication_Practice.Models.Genre>

@{
    ViewData["Title"] = "Index";
}

<h2>Genres Home</h2>

<p>
    @if (User.IsInRole("Admin"))
    {
        <a asp-action="Create">Create New</a>
    }
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @if (User.IsInRole("Admin"))
                {
                    <a asp-action="Edit" asp-route-id="@item.GenreID">Edit</a>
                    <span>|</span>
                    <a asp-action="Details" asp-route-id="@item.GenreID"> Details </a>
                    <span>|</span>
                    <a asp-action="Delete" asp-route-id="@item.GenreID"> Delete </a>
                }
                else
                {
                    <a asp-action="Details" asp-route-id="@item.GenreID"> Details </a>
                }
              
            </td>
        </tr>
}
    </tbody>
</table>
